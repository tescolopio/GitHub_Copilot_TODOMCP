# Workspace configuration
WORKSPACE_PATH=/path/to/your/workspace
CONFIG_PATH=/path/to/config/config.json

# Server configuration
MCP_SERVER_PORT=3000
LOG_LEVEL=info

# Feature flags
ENABLE_AUTO_CONTINUE=false
ENABLE_GIT_OPERATIONS=false

# SECURITY WARNING: Never commit real paths or credentials
# This file is an example only